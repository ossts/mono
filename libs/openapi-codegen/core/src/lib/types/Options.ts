export interface Options {
  /**
   * Path to input file or http endpoint URL for OpenAPI schema
   */
  input: string;
  /**
   * Destination path to which resulting schema should be writter
   */
  output: string;
}
