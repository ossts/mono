export const knownGlobalHelpers = {
  <%- generators.map(generator => `
    '${generator.propertyName}': async () => (await import('@ossts/codegen/generators/${generator.name}')).helpers.globalHelpers
  `).join(','); %>
};
