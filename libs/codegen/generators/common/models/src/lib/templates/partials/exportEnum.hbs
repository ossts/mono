{{#utilsIfdef description deprecated}}
/**
{{#if description}}
 * {{{utilsEscapeComment description}}}
{{/if}}
{{#if deprecated}}
 * @deprecated
{{/if}}
 */
{{/utilsIfdef}}
export enum {{{name}}} {
	{{#each enum}}
	{{#if description}}
	/**
	 * {{{utilsEscapeComment description}}}
	 */
	{{/if}}
	{{#utilsContainsSpaces name}}
	'{{{name}}}' = {{{value}}},
	{{else}}
	{{{name}}} = {{{value}}},
	{{/utilsContainsSpaces}}
	{{/each}}
}