{{#utilsIfSome description deprecated}}
/**
{{#if description}}
 * {{{utilsEscapeComment description}}}
{{/if}}
{{#if deprecated}}
 * @deprecated
{{/if}}
 */
{{/utilsIfSome}}
export enum {{{utilsNameWithNS name}}} {
	{{#each enum}}
	{{#if description}}
	/**
	 * {{{utilsEscapeComment description}}}
	 */
	{{/if}}
	{{#utilsContainsSpaces name}}
	'{{{name}}}' = {{{value}}},
	{{else}}
	{{{name}}} = {{{value}}},
	{{/utilsContainsSpaces}}
	{{/each}}
}

export namespace {{{utilsNameWithNS name}}} {
	{{>exportNoPrimaryKey}}
}