{{#utilsIfdef description deprecated}}
/**
{{#if description}}
 * {{{utilsEscapeComment description}}}
{{/if}}
{{#if deprecated}}
 * @deprecated
{{/if}}
 */
{{/utilsIfdef}}
export type {{{name}}} = {{>utilsType parent=name}};
{{#if enums}}
{{#unless @root.settings.useUnionTypes}}

export namespace {{{name}}} {

	{{#each enums}}
	{{#utilsIfdef description deprecated}}
	/**
	{{#if description}}
	 * {{{utilsEscapeComment description}}}
	{{/if}}
	{{#if deprecated}}
	 * @deprecated
	{{/if}}
	 */
	{{/utilsIfdef}}
	export enum {{{name}}} {
		{{#each enum}}
		{{{name}}} = {{{value}}},
		{{/each}}
	}

	{{/each}}

}
{{/unless}}
{{/if}}