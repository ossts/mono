{{#utilsIfdef description deprecated}}
/**
{{#if description}}
 * {{{utilsEscapeComment description}}}
{{/if}}
{{#if deprecated}}
 * @deprecated
{{/if}}
 */
{{/utilsIfdef}}
export type {{{name}}} = {
	{{#each properties}}
	{{#utilsIfdef description deprecated}}
	/**
	{{#if description}}
	 * {{{utilsEscapeComment description}}}
	{{/if}}
	{{#if deprecated}}
	 * @deprecated
	{{/if}}
	 */
	{{/utilsIfdef}}
	{{>utilsReadOnly}}{{{name}}}{{>utilsRequired}}: {{>utilsType parent=../name}};
	{{/each}}
};
{{#if enums}}
{{#unless @root.settings.useUnionTypes}}

export namespace {{{name}}} {

	{{#each enums}}
	{{#if description}}
	/**
	 * {{{utilsEscapeComment description}}}
	 */
	{{/if}}
	export enum {{{name}}} {
		{{#each enum}}
		{{{name}}} = {{{value}}},
		{{/each}}
	}

	{{/each}}

}
{{/unless}}
{{/if}}