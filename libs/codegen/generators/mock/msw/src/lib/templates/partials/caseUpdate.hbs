case 'update': {
	if (!requestParams.model) return getNotImplementedResponse();
	
	allFaker.updateEntity(
		requestParams.model,
		body,
		// params.persistence.initializerParams?.generatorParams
	);

	return getVoidResponse();
}