case 'delete': {
	if (!requestParams.model) return getNotImplementedResponse();

	const id = +(pathParams?.[idProp ?? ''] ?? '');

	const [isRemoved, message]  = allFaker.removeEntityIfAllowed(requestParams.model, id);

	if (!isRemoved) return getBadRequestResponse(message);

	if (variant.isVoid) return getVoidResponse();

	return HttpResponse.json(null);
}