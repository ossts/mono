{{~#if link~}}
{
	[faker.string.alpha({
		length: {
			min: 5,
			max: 10
		}
	})]: {{>fakerOptionalAndNullableStart}}{{#with link}}{{>fakerStart}}{{/with}}{{>fakerType link hasWrapperType=true}}{{>fakerOptionalAndNullableEnd}}
}
{{~else~}}
{
	[faker.string.alpha({
		length: {
			min: 5,
			max: 10
		}
	})]: {{>fakerOptionalAndNullableStart}}{{#with link}}{{>fakerStart}}{{/with}}{{>fakerBase}}{{>fakerOptionalAndNullableEnd}}
}
{{~/if~}}