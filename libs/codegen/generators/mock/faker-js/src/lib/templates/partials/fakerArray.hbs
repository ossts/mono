{{~#if link~}}
faker.helpers.arrayElements([
	{{#with link}}{{>fakerStart}}{{/with}}{{>fakerType link}}
]{{>fakerArrayParams}})
{{~else~}}
faker.helpers.arrayElements(([
	{{>fakerReference}}
]
{{!-- This is required to filter out possible "null" values caused by recursive calls --}}
// eslint-disable-next-line @typescript-eslint/no-explicit-any
.filter(Boolean) as any){{>fakerArrayParams}})
{{~/if~}}